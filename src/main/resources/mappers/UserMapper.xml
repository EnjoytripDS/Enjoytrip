<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.enjoytrip.user.dao.UserDao">
  <resultMap type="User" id="userMap">
    <result column="user_id" property="id"/>
    <result column="user_email" property="email"/>
    <result column="user_password" property="password"/>
    <result column="user_nickname" property="nickname"/>
    <result column="user_createtime" property="createtime"/>
    <result column="user_updatetime" property="updatetime"/>
  </resultMap>
  <insert id="insert" parameterType="User">
    INSERT INTO user (user_email, user_password, user_nickname, user_createtime, user_updatetime)
    VALUES (#{email}, #{password}, #{nickname}, #{createtime}, #{updatetime});
  </insert>

  <select id="findById" parameterType="int" resultMap="userMap">
    SELECT user_id, user_email, user_password, user_nickname, user_createtime, user_updatetime
    FROM user
    WHERE user_id = #{id}
  </select>

  <update id="update" parameterType="User">
    UPDATE user
    SET user_email = #{email}, user_nickname = #{nickname}, user_password = #{password}, user_updatetime = now()
    WHERE user_id = #{id}
  </update>

</mapper>