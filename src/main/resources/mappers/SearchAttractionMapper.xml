<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.enjoytrip.mapper.SearchAttractionMapper">
  <select id="getGugun" parameterType="int" resultType="List">
    select gugun_name
    from gugun
    where sido_code = #{sidoCode}
  </select>
</mapper>